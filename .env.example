PORT=3000
DB_SERVER=localhost
DB_DATABASE=PhonebookDb
DB_USER=sa
DB_PASSWORD=YourStrongPassword
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true
DB_PORT=1433
ADMIN_USER=admin
ADMIN_PASSWORD=admin

# Only needed when the browser UI is served from a different origin than the
# API (not needed for Vite dev proxy, Nginx, or Caddy single-URL setups).
# CORS_ORIGIN=http://localhost:5173
